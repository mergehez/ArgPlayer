<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="ArgPlayerTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="buttonStyle">@style/ArgPlayerButton</item>
    </style>

    <style name="ArgPlayerButton">
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:scaleType">center</item>
        <item name="android:background">@drawable/back_imagebutton</item>
    </style>
</resources>